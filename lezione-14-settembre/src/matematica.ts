//export perchè voglio visualizzarla dall'esterno.
export function somma(a: number, b: number): number {
  return a + b;
}
// :numer mi specifica cosa restituisce la mia funzione, in questo caso un numero.
